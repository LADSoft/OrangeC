%macro	db	0+
    [db %1]
%endmacro

%macro	resb 0+
    [resb %1]
%endmacro

%macro	dw	0+
    [dw %1]
%endmacro

%macro	resw 0+
    [resw %1]
%endmacro

%macro	dd	0+
    [dd %1]
%endmacro

%macro	resd 0+
    [resd %1]
%endmacro

%macro	dq	0+
    [dq %1]
%endmacro

%macro	resq 0+
    [resq %1]
%endmacro

%macro	dt	0+
    [dt %1]
%endmacro

%macro	rest 0+
    [rest %1]
%endmacro

%macro	section 0+
    [section %1]
%endmacro

%macro	align 0+
    [align %1]
%endmacro

%macro	import 0+
    [import %1]
%endmacro

%macro	export 0+
    [export %1]
%endmacro

%macro	global 0+
    [global %1]
%endmacro

%macro	extern 0+
    [extern %1]
%endmacro

%macro equ 0+
    [equ	%1]
%endmacro

%macro incbin 0+
    [incbin	%1]
%endmacro

%macro times 0+
    [times %1]
%endmacro

%macro	DB	0+
    [db %1]
%endmacro

%macro	RESB 0+
    [resb %1]
%endmacro

%macro	DW	0+
    [dw %1]
%endmacro

%macro	RESW 0+
    [resw %1]
%endmacro

%macro	DD	0+
    [dd %1]
%endmacro

%macro	RESD 0+
    [resd %1]
%endmacro

%macro	DQ	0+
    [dq %1]
%endmacro

%macro	RESQ 0+
    [resq %1]
%endmacro

%macro	DT	0+
    [dt %1]
%endmacro

%macro	REST 0+
    [rest %1]
%endmacro

%macro	SECTION 0+
    [section %1]
%endmacro

%macro	ALIGN 0+
    [align %1]
%endmacro

%macro	IMPORT 0+
    [import %1]
%endmacro

%macro	EXPORT 0+
    [export %1]
%endmacro

%macro	GLOBAL 0+
    [global %1]
%endmacro

%macro	EXTERN 0+
    [extern %1]
%endmacro

%macro EQU 0+
    [equ	%1]
%endmacro

%macro INCBIN 0+
    [incbin	%1]
%endmacro

%macro TIMES 0+
    [times %1]
%endmacro
